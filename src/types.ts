export interface CategoryAxis {
    showAxis: boolean;
    axisLabel: string;
    indicator: string;
  }
export interface LinearAxis  {
    showAxis: boolean;
    showGridlines: boolean;
    indicator: string;
    axisLabel: string;
  }
